<!DOCTYPE html>
 <html xmlns:th="https://www.thymeleaf.org" xmlns:sec="http://www.thymeleaf.org/extras/spring-security">    
     <head>
         <div th:replace="~{fragment :: meta}"></div>   
              
         <div th:replace="~{fragment :: styles}"></div>
           
         <title>カテゴリー作成</title>   
     </head>
     <body>
         <div class="samuraitravel-wrapper">
             <!-- ヘッダー -->
             <div th:replace="~{fragment :: header}"></div>
             
             <main>
                 <div class="container samuraitravel-container pb-5">
                     <div class="row justify-content-center">
                         <div class="col-xxl-9 col-xl-10 col-lg-11">
                             <nav class="my-3" style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
                                 <ol class="breadcrumb mb-0"> 
                                     <li class="breadcrumb-item"><a th:href="@{/}">ホーム</a></li>  
									 <li class="breadcrumb-item"><a th:href="@{/admin/category}">カテゴリ管理</a></li>                   
                                     <li class="breadcrumb-item active" aria-current="page">カテゴリー作成</li>
                                 </ol>
                             </nav> 
                             
                             <h1 class="mb-3 text-center">カテゴリー一覧</h1>  
             
							 <div th:if="${successMessage}" class="alert alert-info">
				 				<span th:text="${successMessage}"></span>
				 		 	  </div>
							  
							  
				  		<!--カテゴリ検索-->
							  <div>
								<form method="get" th:action="@{/admin/category}">
									<input type="text"name="keyword" th:value="${keyword}"  placeholder="キーワード">
									<input type="submit" value="検索">
								</form>
							  </div>
							  
						<!--カテゴリ登録-->
							<div>
								<a href="@{/admin/create}">カテゴリ登録</a>
							</div>
							<div>
								<form method="post" th:action="@{/admin/category/create}" th:object="${categoryForm}">
									<div th:if="${#fields.hasErrors('name')}" class="text-danger small mb-2" th:errors="*{name}"></div>
									
									<input type="text" th:field="*{name}" placeholder="キーワードの新規登録">
									<input type="submit" value="登録">
								</form>
							  </div>
						
							  
						<!--カテゴリなし-->
							 <div th:if="${categoryPage.isEmpty()}" class="row justify-content-center" th:text="カテゴリーがありません"> </div>
							
						<!--カテゴリ情報-->
							<div class="mb-5">
								<span th:each="category:${categoryPage}"><a href="#" data-bs-toggle="modal" th:data-bs-target="${'#modalCategory'+category.getId()}" th:text="${category.getName()}"  class="btn btn-outline-secondary btn-sm me-1 mb-2"></a>
									
									<!--モーダル-->
									<div class="modal fade" th:id="${'#modalCategory'+category.getId()}" tabindex="-1" aria-labelledby="'modalCategoryLabel'+categor.getId()">
										<div class="modal-dialog">
											<div class="modal-content">
												<div class="modal-header">
													
												</div>
											</div>
										</div>
										
									</div>
								</span>
							</div>
	                                  
      
                                        
                     </div>
                 </div>  
             </main>
             
           
         </div>    
		 <!-- フッター -->
		 <div th:replace="~{fragment :: footer}"></div>
         <div th:replace="~{fragment :: scripts}"></div>  
   </body>
 </html>